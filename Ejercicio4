//Escribe un programa que indique si un número entero leído por teclado se puede expresar 
//como el cuadrado de un número entero. Por ejemplo, el número 9 se puede expresar como el 
//cuadrado de 3, mientras que el 8 no. (Pista, no hace falta utilizar repetición, utilizar librería matemática).

import java.util.Scanner;

public class Ejercicio4 {

    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);
        double a;

        System.out.println("Introduce un valor entero");
        a = sc.nextInt();

        if (Math.sqrt(a) > (int) Math.sqrt(a)){
            System.out.println("El número introducido no tiene una raiz entera");

        }else{
            System.out.println("El número que has introducido tiene como raiz cuadrada " + Math.sqrt(a));
        }

    }

}
